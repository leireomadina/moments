---
import { getCollection } from 'astro:content'
import Layout from '@layouts/BaseLayout.astro'
const allAlbums = await getCollection('albums')
---

<Layout>
	<h1>My albums</h1>
	<ul>
		{
			allAlbums.map(({ id, data: { title, date } }) => (
				<li>
					<a href={`/albums/${id}`}>
						{title} - {date}
					</a>
				</li>
			))
		}
	</ul>
</Layout>
